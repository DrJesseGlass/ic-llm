[package]
name = "qwen3_backend"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Track ic-dev-kit-rs v0.1.0: ic-cdk 0.20, candid 0.10.31
candid = "0.10.31"
ic-cdk = "0.20"
ic-stable-structures = "0.7.2"
serde = { version = "1.0", features = ["derive"] }

# Dev kit, pinned to tag v0.1.0
ic-dev-kit-rs = { git = "https://github.com/DrJesseGlass/ic-dev-kit-rs", tag = "v0.1.0", features = ["text-generation", "storage", "candle", "telemetry"] }
#ic-dev-kit-rs = { path = "../../../../ic-dev-kit-rs", features = ["text-generation", "storage", "candle", "telemetry"] }

# crates.io candle 0.11; workspace [patch.crates-io] redirects to the fork.
candle-core = { version = "0.11", default-features = false }
candle-nn = { version = "0.11", default-features = false }
candle-transformers = { version = "0.11", default-features = false }

# crate does not include examples
tokenizers = { version = "0.22", default-features = false, features = ["fancy-regex"] }

# Utilities
anyhow = "1.0"
canbench-rs = { version = "0.6", optional = true }

[features]
default = []
canbench-rs = ["dep:canbench-rs"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("telemetry"))'] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"